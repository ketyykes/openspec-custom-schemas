# Specification: <capability-name>

<!--
  Delta operations: use ## headers exactly as below.
  Do NOT embed ASCII diagrams or visualizations here; all visuals go to overview.md.
  Only include the sections you actually use — delete the rest (including the heading).
-->

## ADDED Requirements

### Requirement: <name>

The system SHALL <observable behavior description>.

#### Scenario: <scenario name>

- **WHEN** <triggering condition>
- **THEN** <expected outcome>

#### Scenario: <another scenario>

- **WHEN** <...>
- **THEN** <...>

<!--
  Add the sections below ONLY if this delta needs them.
  Each section must follow the same `### Requirement:` + `#### Scenario:` format.

  ## MODIFIED Requirements
    - Paste the ENTIRE existing requirement block (header + all scenarios) and edit.
    - Header text must match the existing requirement exactly (whitespace-insensitive).

  ## REMOVED Requirements
    - Each entry needs **Reason** and **Migration** fields.

  ## RENAMED Requirements
    - Use FROM: / TO: lines (name change only — content unchanged).
-->
